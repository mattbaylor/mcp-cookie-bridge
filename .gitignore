node_modules/
dist/

# User-specific config (contains cookie names specific to your project)
config.json
extension/config.json

# User-specific extension manifest (host_permissions name your source host)
extension/manifest.json

# Persisted cookie data (contains secrets)
cookies.json
